# Provenance

- **Upstream**: [`kitlangton/skills`](https://github.com/kitlangton/skills), path `skills/effect/`
- **Vendored SHA**: `30dee8607214c893dd89f6eee65c669ef3dce8c9` (pinned, not HEAD — upstream may have moved since)
- **Vendor date**: 2026-07-15
- **License**: MIT, copied verbatim to `LICENSE` in this directory
- **Files vendored**: `SKILL.md`, `references/{CACHING,CONFIG,HTTP_CLIENTS,SCHEDULING,SCHEMA,SERVICES_LAYERS,STREAMS,TESTING}.md` (8 reference docs, matching upstream's tree exactly — no files added or dropped)
- **This repo's resolved pin at vendor time**: `effect@4.0.0-beta.98` (upstream's skill was reviewed by its author against beta.97 per its `compatibility` frontmatter note; one patch apart)

## Re-vendor runbook

1. Pick the new upstream SHA (or HEAD) to re-vendor from.
2. Fetch it fresh into a scratch dir: `gh api 'repos/kitlangton/skills/contents/<path>?ref=<new-sha>'` per file (base64-decode), or `git clone` + `git show`.
3. Diff the NEW pristine fetch against the OLD pristine copy this repo captured at `30dee8607214c893dd89f6eee65c669ef3dce8c9` — **not** against this directory's current (adapted) files — to see what upstream actually changed. (The old pristine tree isn't committed here; re-fetch it at the recorded SHA above if you need it, or reconstruct it by reverse-applying `vendor.patch` to this directory.)
4. Re-apply this file's adaptation list to the new pristine tree, item by item, checking whether each adaptation is still needed (an upstream fix might have obsoleted one; an upstream API rename might invalidate a compile-fix).
5. Regenerate `vendor.patch` (`diff -ruN <new-pristine> .claude/skills/effect`, excluding `PROVENANCE.md` and `vendor.patch` itself).
6. Bump this file's SHA/date, run `bun test tests/skills-verify.test.ts`, then `bun run scripts/skills-verify.ts --stamp` once it's green apart from the version stamp.

## Adaptation list (every departure from the pristine upstream tree)

1. **Frontmatter** (`SKILL.md`): added `vendored-from: kitlangton/skills@30dee8607214c893dd89f6eee65c669ef3dce8c9`; added `verified-against: effect@pending` as a placeholder for `scripts/skills-verify.ts --stamp` to rewrite to the real resolved version once the gate is green (the stamper only rewrites an *existing* `verified-against` line — see that script's `stampVerifiedAgainst`). Upstream's `name`/`description`/`license`/`compatibility` keys kept verbatim.
2. **Source Rule** (`SKILL.md`): added one sentence naming this repo's actual resolved pin (`effect@4.0.0-beta.98`, read from `package.json`/`bun.lock`) and noting that every fenced example in `references/*.md` is typechecked in-process against that exact pin by `scripts/skills-verify.ts` — upstream's Source Rule text otherwise generic/unpinned, kept as-is.
3. **Do Nots** (`SKILL.md`): appended one line pointing at `CONFIG.md`'s new Do-Not about `Number(process.env.X) || default` (see item 12).
4. **Every `ts` fenced block, all 8 reference docs**: added a required `id=` (kebab-case, descriptive of the example) per the skills-verify fence grammar. None of upstream's blocks carried an `id=` — this repo's gate requires one on every verified block.
5. **`file=` grouping for progressive multi-file examples**:
   - `SCHEMA.md`: `user-schema.ts` (Records) + `field-reuse.ts` (Field And Contract Reuse) grouped so `field-reuse.ts` can `import { User } from "./user-schema.js"` — upstream's prose already implied this dependency (`User.fields.name`) without showing the import.
   - `SERVICES_LAYERS.md`: `user-repo.ts` (Module Surface: Interface + Service + layer + errors + self-export) + `consumer.ts` (Consumers use the module namespace) grouped so `consumer.ts` can `import { UserRepo } from "./user-repo.js"`, matching upstream's own two-block presentation of producer then consumer.
6. **Added `import { ... } from "effect"` (and `effect/unstable/sql`, `effect/unstable/http`) to every verified block.** Upstream's snippets never carry imports (they're excerpts, not standalone files); this repo's gate synthesizes each block as a real `.ts` file and typechecks it, so every block needed its imports added. This is boilerplate, not a content change — it does not alter what any example teaches.
7. **Added minimal local type/value stand-ins (`interface`, small `class ... extends Context.Service`/`Schema.TaggedErrorClass`, or `declare const x: T`) for identifiers upstream's snippets reference but never define** (e.g. `UserId`/`User` in `SERVICES_LAYERS.md`'s module-surface example, `RunId` in `SCHEMA.md`'s tagged-union example, `AttachmentRef`/`api`/`attachmentError` in the `Effect.fn` example, `Gateway`/`handleEvent` in `STREAMS.md`, `ProviderEvent`/`ProviderError`/`ProviderStatus` in the "good service shape" example, `reconcile`/`ProjectionError`/`Target` in `SCHEDULING.md`, `resolveUncached` in `CACHING.md`, `apiClientLayer`/`ApiClient` in `CACHING.md`, `Configuration`/`makeClient`/`Client` in `CONFIG.md`). Each stand-in is named and shaped to match what the surrounding prose already implies (an ID type is a branded string, a service is a `Context.Service`, an error is a `Schema.TaggedErrorClass`) — none of it changes the pattern being taught, only makes the illustration self-contained enough to compile.
8. **Genuine compile fixes (upstream would not have typechecked as written even with imports added)** — tracked separately because these are signal about upstream doc quality, not routine adaptation:
   - `SERVICES_LAYERS.md`, Module Surface: the `get` method body was `// ...` with no `yield`/`return`, which infers `Effect<void, never, never>` — incompatible with the declared `Interface.get: (id) => Effect.Effect<User, NotFound | PersistenceError>`. Changed to `return yield* Effect.fail(new NotFound({ id }))`, a real (if placeholder) implementation matching the declared type.
   - `SERVICES_LAYERS.md`, Operation Error Helpers: upstream's text calls `PersistenceError.make` on a `Schema.TaggedErrorClass`-produced class, but that class shape only exposes a `new` constructor (checked against `node_modules/effect/dist/Schema.d.ts`'s `TaggedErrorClass` declaration — no static `make`). Changed the curried helper's constructor argument from `PersistenceError.make` to `(input) => new PersistenceError(input)`, and wrote a concrete (correct) `operationError` implementation instead of leaving it as an undefined import, since the whole point of this section is "prefer a shared curried helper" — showing one that actually type-checks is strictly more useful than an opaque `declare const`.
   - `STREAMS.md` / `SERVICES_LAYERS.md` Long-Lived Work: upstream's snippet reads `Events.Service` (the module-namespace-self-export style from `SERVICES_LAYERS.md`'s Module Surface section, applied to a service the example never defines). Simplified to `yield* Events` (the tag itself), since this doc's own examples don't otherwise use the self-export style (which is demoted to an optional aside per item 10 below) and introducing a second self-exporting namespace module here just to preserve the dotted form would be a much larger addition than the point being taught (forking a stream during layer acquisition).
9. **`TESTING.md`: two blocks (`it.effect("finds a user", ...)`, `it.effect("publishes exactly once", ...)`) marked `no-verify`.** `it.effect`/`it.live` come from `@effect/vitest`, which this repo does not depend on (`package.json` has no `@effect/vitest`; the repo's test runner is `bun:test`). This is a real, checked gap, not laziness — see the note added at the top of `TESTING.md`'s Defaults section explaining that the underlying pattern (test layers, `TestClock`, `Deferred`/`Queue`/`Latch`/`Ref` synchronization) still applies verbatim inside a `bun:test` body via `Effect.runPromise`; only the Vitest-specific `it.effect(...)` wrapper itself doesn't resolve. Rewriting these two blocks to a `bun:test`-native shape would materially change what upstream is teaching (a `@effect/vitest`-specific integration, not a generic pattern), so `no-verify reason="..."` was used instead of forcing a rewrite. `NO_VERIFY_BASELINE["effect"] = 2` in `scripts/skills-verify.ts` accounts for these.
10. **Self-export module-namespace pattern demoted to an optional aside** (`SERVICES_LAYERS.md`, Module Surface section). Upstream presents `export * as UserRepo from "./user-repo.js"` as its own opinionated default and already flags the resulting `UserRepo.UserRepo === UserRepo` self-reference as "unusual." Checked this repo's actual convention: `git grep -n '^export \* as' src/` returns zero matches — this codebase does not use the pattern anywhere. Kept the pattern's working example (it's still part of the compiled `user-repo.ts`/`consumer.ts` pair, since upstream's own prose walks through it as the primary illustration) but added a callout block making explicit that it's optional and not this repo's house style, and converted two follow-up illustrative fragments (a bare sibling-import snippet and a bare folder-barrel snippet, both incomplete without their sibling context) from code fences into the surrounding prose, since forcing them to compile standalone would have required inventing a folder/barrel structure well beyond what they were illustrating.
11. **Added `Do Nots` items folding in this repo's own recorded Effect v4 gotchas**:
    - `CONFIG.md`: `Number(process.env.X) || default` silently discards a legitimate falsy value (`0`, `false`-shaped numeric configs) because `0 || default` evaluates to `default`. This hit 34 call sites in this repo's own Effect v4 migration (recorded in this repo's operator memory under the effect-setup lesson) before being caught. Added as the primary new Do-Not, with the `Config.number(...).pipe(Config.withDefault(...))` fix named explicitly.
    - `CACHING.md` and `SCHEDULING.md`: added one-line pointers back to `CONFIG.md`'s Do-Not for the same falsy-eating bug class, scoped to cache TTL/capacity and retry/backoff/poll-interval config reads specifically, since those are exactly the numeric-with-a-legitimate-zero config shapes this bug class targets.
    - No other gotcha was added beyond the one above and its two pointers — the task scope requires every additional gotcha to be independently verifiable from this repo's own `plans/` or code, and no second one was found that was both genuine and specific to the branches these reference docs cover.
12. **Nothing else changed.** Every other line of prose, every bullet list, every piece of non-code guidance is upstream's text verbatim (including capitalization, wording, and section order).

## Compile-fix count (signal about upstream quality)

3 blocks needed a genuine logic/type fix beyond "add imports and define referenced-but-undefined identifiers" to compile at `effect@4.0.0-beta.98` (item 8 above: the unfinished `get` method body, the nonexistent `PersistenceError.make` static, and the `Events.Service` dotted reference to an undefined self-export). Everything else that needed touching was purely additive scaffolding (imports, minimal stand-in types) that upstream's excerpt format never carried in the first place.
