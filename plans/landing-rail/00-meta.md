# Landing-rail campaign — meta

STATUS: open
PRIORITY: p0
MODE: afk (execution override recorded on the map issue; hitl only where a ticket says so)

Destination: the rail lands its own PRs with full receipts (measured reviewer precision
cited) for 2 weeks — honesty debts fixed, moat loops closed, receipt surface live, rail
extracted from squad-manager.ts, codex harness verified, GitHub App wedge spiked — ending in
a gate review with the receipts as evidence.

Map: the `wayfinder:map` issue on lmvdz/glance (see Ledger row for number). Tracker of
record: GitHub issues. This dir carries the goal (GOAL.md), the settled brief (BRIEF.md),
and the ledger below; per-decision detail lives on the tickets, never here.

## Ledger

| date | event |
|---|---|
| 2026-08-04 | Campaign chartered. Destination + tracker fixed by Lars (full ladder + 2-week receipts; GitHub issues). Market scan + capability audit recorded as settled inputs in BRIEF.md. |
