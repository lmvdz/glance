# daemon gaps (omp-squad companion)

STATUS: cancelled
PARENT: hub-shell

Named, not designed here; each hub control ships gated on field presence (degrades on older daemons, like ladderPriority absent→idle): thread TITLE (first-prompt snippet) + lastActivityAt on the agents DTO (kills the 'New thread' constant); GET /api/projects; per-thread model/effort/access on DTO + mutation endpoints; POST /api/agents/:id/land (Commit&push); per-turn diff attribution. REPO: omp-squad. PARALLEL anytime.

## Resolution
Superseded 2026-07-22 by plans/the-room — the thesis (chat as root, expert surfaces as modes)
is absorbed as HubShell-in-webapp (the-room DESIGN.md, decision "Shell"); the method (executing
inside glance-desktop) is retired with the fork (unused, non-working — Lars directive). See
plans/the-room/07-hubshell-root.md for the successor concern.
