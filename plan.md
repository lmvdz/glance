# OMPSQ-480 — Test-only implementation plan

1. Audit the existing coverage in `tests/channel-membership-fanout.test.ts`, `tests/push-server.test.ts`, and `tests/dal-store.test.ts` against all nine requested cases. The current suite already contains named coverage for private wire fan-out, push scoping/unbound subscriptions, private artifacts after reaping, public dead placeholders, failed membership creation, over-fetch search, DbStore membership behavior, and private-command acknowledgements; preserve it and identify only real omissions.
2. For any gap, add adversarial tests—not implementation changes—that fail if the relevant membership enforcement is deleted: private `channel-entry` and `transition` delivery must reach members only; non-member HTTP reads/search must deny or omit private data; and creator fallback, search pagination, and DbStore revocation must remain independently pinned.
3. Ensure the four concern acceptance criteria have explicit end-to-end assertions: C captures zero private socket frames, C’s private search/API reads are 403/empty, B loses the next event after revocation, and org-public delivery remains unaffected. Record whether each was pre-existing or added.
4. Where safe to isolate, temporarily remove the protected enforcement, confirm each newly added test goes red, then restore it unchanged. If this exposes a production defect, retain a failing or documented skipped test and report it; do not alter production code.
5. Run only `bun test tests/channel-membership-fanout.test.ts tests/dal-store.test.ts tests/push-server.test.ts tests/projection-routing.test.ts` plus any new test file, verify an actual pass count, then commit test-only changes in logical groups. Do not push or open a PR.
